#profile.container
  .row
    .span3.text-center
      = image_tag @user.avatar_url(:profile), class: 'img-circle'
      %h2
        = image_tag @user.native_language.flag_path(32)
        = "#{@user.first_name} #{@user.last_name}"      
      - if @user == current_user
        %p 
          = link_to edit_profile_users_path do
            Edit profile
      %p
        = link_to chats_create_path(:guest_id => @user.id), :class => "btn btn-primary", :method => :post do
          %i.icon-facetime-video
          &nbsp;
          Start conversation
    .span8
  
      %p
        